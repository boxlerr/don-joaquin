// Stub vacío de `server-only` para los tests: en vitest no corremos en un
// entorno RSC, así que el guard real (que tira error al importarse fuera de un
// Server Component) se reemplaza por este no-op.
export {};
